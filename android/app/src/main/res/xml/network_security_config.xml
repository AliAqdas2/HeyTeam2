<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTPS connections to portal.heyteam.ai -->
    <!-- This configuration trusts both system and user certificates for this domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">portal.heyteam.ai</domain>
        <domain includeSubdomains="true">heyteam.ai</domain>
        <trust-anchors>
            <!-- Trust system certificates (standard CA certificates) -->
            <certificates src="system" />
            <!-- Trust user-installed certificates (helps with custom/self-signed certs) -->
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
    
    <!-- Base configuration: use system trust anchors for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
